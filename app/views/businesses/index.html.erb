<p style="color: green"><%= notice %></p>

<div id="businesses">
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <div class="col">
      <% @businesses.each do |business| %>
        <%= render business %>
      <% end %>
    </div>
  </div>
</div>
